# PostgreSQL password (change in production)
POSTGRES_PASSWORD=wordle_secure_password

# Must be at least 32 random characters in production
JWT_SECRET=change-me-to-a-long-random-secret-in-production

# Admin account created at first boot
ADMIN_EMAIL=admin@wordle.local
ADMIN_PASSWORD=changeme123
